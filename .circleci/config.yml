# version: 2.1

# jobs:
#   build:
#     working_directory: ~/circleci-python
#     docker:
#       - image: "circleci/python:3.6.4"
#     steps:
#       - checkout
#       - run:
#           command: sudo apt install python3-pip
#       - run:
#           command: pip3 install parameterized
#   test:
#     working_directory: ~/circleci-python
#     docker:
#       - image: "circleci/python:3.6.4"
#     steps:
#       - checkout
#       - run:  python3 test_board.py
#       - run:  python3 test_piece.py
#       - run:  python3 test_bot.py

# workflows:
#   build_and_test:
#     jobs:
#       - build
#       - test:
#           requires:
#             - build


version: 2.1


orbs:
  python: circleci/python@1.5.0
jobs:
  build-and-test: # This is the name of the job, feel free to change it to better match what you're trying to do!
    docker:
      - image: cimg/python:3.10.2
    steps:
      - checkout
      - python/install-packages:
          pkg-manager: pip
          # app-dir: ~/project/package-directory/  # If you're requirements.txt isn't in the root directory.
          # pip-dependency-file: test-requirements.txt  # if you have a different name for your requirements file, maybe one that combines your runtime and test requirements.
      - run: sudo apt-get install -y python-parameterized
      - run:
          # This assumes pytest is installed via the install-package step above
          command: python3 test_board.py
      - run:
          command: python3 test_piece.py
      - run:
          command: python3 test_bot.py


workflows:
  sample: # This is the name of the workflow, feel free to change it to better match your workflow.
    # Inside the workflow, you define the jobs you want to run.
    jobs:
      - build-and-test